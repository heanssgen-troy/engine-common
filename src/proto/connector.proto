syntax = "proto3";

message Registration {
  enum RegistrationMode {
    CLIENT = 0;
    ENGINE = 1;
  }

  string name = 1;
  RegistrationMode mode = 2;
  string id = 3;
}

message Disconnect {
  string id = 1;
}